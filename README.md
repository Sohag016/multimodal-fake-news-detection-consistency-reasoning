# multimodal-fake-news-detection-consistency-reasoning
Multimodal fake news detection on Fakeddit (6-way) using RoBERTa + ResNet-50 + CLIP + OCR + XGBoost with novel Consistency Score reasoning | Research Paper Implementation
